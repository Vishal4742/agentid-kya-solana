VITE_SOLANA_RPC_ENDPOINT=https://api.devnet.solana.com
# Set this when the metadata API is deployed on a separate host.
# If omitted, the frontend uses the current site origin and requests /metadata/:name.json.
# Example: VITE_METADATA_BASE_URL=https://agentid-metadata-api.vercel.app
VITE_METADATA_BASE_URL=
