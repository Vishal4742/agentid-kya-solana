{
    "version": 2,
    "builds": [
        {
            "src": "metadata/[agentId].ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/metadata/(.*)",
            "dest": "/metadata/[agentId].ts?agentId=$1"
        }
    ]
}